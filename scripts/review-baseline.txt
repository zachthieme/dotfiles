# Accepted-failing rubric criteria (see docs/review-rubric.md + scripts/review.sh).
#
# review.sh fails CI on a criterion that breaks and ISN'T listed here (a
# regression), and on a listed criterion that starts PASSING (stale — remove it,
# so the fix can't silently regress later). To accept a new open item, add its
# id here WITH a reason. To close one, fix it and delete the line.
#
# id    reason
# (none — all mechanical criteria currently pass)
