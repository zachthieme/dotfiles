function outlook-here --description="Bring the Outlook PWA window to the current workspace"
    # Chrome PWA bundle id — same app aero-tidy assigns to 2_Mail
    aero-summon com.google.Chrome.app.faolnafnngnfdaknnbpnkhgohbobgegn
end
